gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# NOTE: key.properties + cc-handoff-release.jks are committed ON PURPOSE so CI
# signs releases with a stable key without secrets. This is a deliberate tradeoff
# for a public sideloaded app (see build.gradle.kts); the default Flutter ignore
# rules for them are removed below.
