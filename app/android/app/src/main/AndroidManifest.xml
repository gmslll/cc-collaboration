<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for release builds to reach the relay (debug/profile manifests
         add this automatically, the release one does not). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- flutter_webrtc/WebRTC network monitor calls ConnectivityManager when a
         peer connection starts; without this Android can throw SecurityException
         and libwebrtc aborts the process. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- In-app update: hand a downloaded APK to the system package installer. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- Voice input (speech_to_text). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Live-Activity-equivalent: a foreground service keeps the relay WebSocket
         alive in the background so the "AI 干活中" ongoing notification updates
         while the user is in another app. dataSync = keep a network connection to
         sync data; targetSdk 34+ requires the granular permission to match. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <application
        android:label="Infinite Agents"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic: the relay is plain http://; Android 9+ blocks
             cleartext by default. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Foreground service backing the "AI 干活中" ongoing notification (the
             Android equivalent of the iOS Live Activity). stopWithTask: the relay
             WebSocket lives in the main isolate, so if the task is swiped away the
             engine dies and there's nothing left to show — stop with it. -->
        <service
            android:name=".LiveActivityService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="true" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- speech_to_text needs to see the on-device speech recognizer. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
